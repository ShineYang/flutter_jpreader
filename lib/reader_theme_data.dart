import 'dart:ui';

class ReaderThemeData{
  Color primaryColor;//theme color
  Color backgroundColor;
  Color titleColor;//appbar title & book title
  Color subTitleColor;//book author

  ReaderThemeData({required this.primaryColor, required this.backgroundColor, required this.titleColor, required this.subTitleColor});
}